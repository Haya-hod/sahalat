// Full app widget tests need Firebase initialization; navigation logic is covered by
// test/stadium_ring_routing_test.dart, gate_a_*.dart, etc.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite anchor (navigation tests run separately)', () {
    expect(true, isTrue);
  });
}
